
# 🖥️ Roomify Server — Classroom/Lab Reservation Backend

> 강의실·실습실 예약 시스템의 **서버(백엔드)** 입니다.  
> 동시 접속 **3명 제한**, **4번째 이후 대기열**, **로그아웃 시 자동 입장**, **중복 로그인 차단**, **파일 동기화**, **외부 IP 접속**을 지원합니다.  
> UI/클라이언트 설명은 메인 프로젝트 README에 정리합니다.

---

## ✨ 주요 기능

- 🧑‍🤝‍🧑 **동시 3명 접속 제한**: 4번째부터 **대기열(Queue)** 등록
- 🔁 **자동 입장**: 접속자 로그아웃/종료 시 대기열 선입선출(FIFO)로 즉시 입장 통지
- 🚫 **중복 로그인 차단**: 활성 세션/대기열 모두에서 동일 ID 감지
- 🗂 **파일 동기화**: 클라이언트의 파일 변경을 서버에 반영
- 🔐 **인증/회원 관리**: 로그인·회원가입, 사용자 정보 조회
- 🌐 **외부 IP 접속 가능**: 방화벽/포트포워딩 설정 시 외부 네트워크 접속

---

## 🧰 사용한 기술

- **TCP 소켓 통신**  
  `ServerSocket.accept()` / `Socket` 기반의 라인 텍스트 프로토콜

- **Per-connection 워커 스레드**  
  연결 1개 = `ClientHandler` 1스레드 (요청 처리·응답·정리)

- **세션 제한(3명) + FIFO 대기열 + 자동 입장**  
  `SessionManager`로 동시 접속 3명 제약, 4번째는 큐 등록, 자리 나면 즉시 승격

- **파일 동기화(서버 반영 + ACK)**  
  `FileSyncManager`가 변경을 서버에 반영하고 `FILE_UPDATE_OK` 응답

---
## 🚀 실행 방법 (CLI)

- NetBeans: Main Class = `Server.ServerMain` → Run(F6)  

> JDK 17+ / Git Bash 기준. IDE(인텔리J/이클립스/넷빈즈)로 실행해도 됩니다.

---
##  🗂파일구조
<pre>

Source Packages/
├─ Client/
│  ├─ FileSyncManager.java   # 파일 동기화: FILE_UPDATE 처리, 서버 반영(옵션: 알림)
│  ├─ RegisterHandler.java   # 회원가입 처리 (저장소 갱신)
│  └─ UserInfoHandler.java   # 사용자 정보 질의/응답
└─ Server/
   ├─ ServerMain.java        # 서버 시작: 포트 열고 접속 수락
   ├─ ClientHandler.java     # 클라 1명 담당: 요청 읽기→처리→응답→정리
   ├─ LoginProcessor.java    # 로그인 검증(ID/PW) → SessionManager 위임
   ├─ SessionManager.java    # ⭐ 동시 3명 제한, 대기열, 자동 입장, 중복 로그인 차단
   └─ ServerView.java        # 서버 IP/포트/로그 출력
</pre>

